<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    >
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".activities_fragments.activity_inner_call.InnerCallActivity"
        android:background="@color/second"
        android:keepScreenOn="true"
        >

        <ImageView
            android:id="@+id/image"
            android:layout_width="150dp"
            android:layout_height="150dp"
            android:src="@drawable/done_logo"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="40dp"/>

        <TextView
            android:id="@+id/tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/image"
            android:layout_marginTop="20dp"
            android:textSize="20sp"
            android:textColor="@color/white"
            android:text="@string/inner_call"
            />

        <com.ebanx.swipebtn.SwipeButton
            android:id="@+id/fab"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/tv"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginTop="30dp"
            android:layout_marginBottom="30dp"
            app:layout_constraintVertical_bias=".8"
            android:layout_marginStart="40dp"
            android:layout_marginEnd="40dp"
            app:inner_text="@string/reply"
            app:inner_text_color="@android:color/white"
            app:inner_text_size="16sp"
            app:inner_text_top_padding="10dp"
            app:inner_text_bottom_padding="10dp"
            app:inner_text_background="@drawable/btn_shape_reply"
            app:button_image_height="50dp"
            app:button_image_width="50dp"
            app:button_image_disabled="@drawable/ic_call3"
            app:button_image_enabled="@drawable/ic_call3"
            app:button_left_padding="15dp"
            app:button_right_padding="15dp"
            app:button_top_padding="15dp"
            app:button_bottom_padding="15dp"
            app:button_background="@drawable/rounded_white"
            app:initial_state="disabled"
            app:has_activate_state="true"

            />





    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
